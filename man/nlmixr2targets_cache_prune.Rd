% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indirection.R
\name{nlmixr2targets_cache_prune}
\alias{nlmixr2targets_cache_prune}
\title{Drop orphaned entries from the \code{nlmixr2targets} indirect cache}
\usage{
nlmixr2targets_cache_prune(
  keep = NULL,
  dry_run = TRUE,
  directory = file.path(targets::tar_config_get("store"), "user/nlmixr2")
)
}
\arguments{
\item{keep}{Character vector of md5 hashes to retain. If \code{NULL}
(default), the helper auto-discovers reachable hashes from the
\code{targets} metadata in the current store. Set to \code{character()} to
treat every cached entry as an orphan.}

\item{dry_run}{Logical. If \code{TRUE} (default), do not delete anything;
just return the hashes that would be removed. If \code{FALSE}, delete.}

\item{directory}{Cache directory; same default as
\code{\link[=nlmixr2targets_cache_status]{nlmixr2targets_cache_status()}}.}
}
\value{
Invisibly, a character vector of the orphan hashes (those that
were removed when \code{dry_run = FALSE}, or would be removed otherwise).
}
\description{
Removes cache entries whose md5 hash is not in \code{keep}. By default
\code{dry_run = TRUE}, so the function reports what would be removed without
actually deleting anything; pass \code{dry_run = FALSE} to delete.
}
\details{
When \code{keep = NULL}, the function reads the \code{targets} metadata in the
current store and treats every value of a built target whose name ends
in \verb{_object_simple} (the hashes produced by \code{\link[=nlmixr_object_simplify]{nlmixr_object_simplify()}})
as reachable. Pass an explicit character vector if you want to override
that, e.g. before calling \code{\link[targets:tar_destroy]{targets::tar_destroy()}} yourself.
}
\seealso{
\code{\link[=nlmixr2targets_cache_status]{nlmixr2targets_cache_status()}}.
}
