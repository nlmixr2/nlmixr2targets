% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indirection.R
\name{nlmixr2_indirect}
\alias{nlmixr2_indirect}
\title{Estimate an nlmixr2 model loading the model from a targets indirect hash storage}
\usage{
nlmixr2_indirect(
  object,
  data,
  est,
  control,
  directory = file.path(targets::tar_config_get("store"), "user/nlmixr2"),
  error = c("stop", "continue")
)
}
\arguments{
\item{object}{Fitted object or function specifying the model.}

\item{data}{nlmixr data}

\item{est}{estimation method (all methods are shown by
`nlmixr2AllEst()`). Methods can be added for other tools}

\item{control}{The estimation control object.  These are expected
to be different for each type of estimation method}

\item{directory}{Cache directory to load the simplified \code{nlmixrui} from.
Defaults to \code{file.path(targets::tar_config_get("store"), "user/nlmixr2")},
mirroring the convention used by \code{targets}' own \code{store = targets::tar_config_get("store")} defaults — the path resolves at
call time against whatever store the user has configured for the
running \code{tar_make()} (e.g. a \code{tempdir()} location set via
\code{targets::tar_config_set()}).}

\item{error}{What should happen if the estimation step throws an error?
\code{"stop"} (the default) lets the error propagate, halting
\code{targets::tar_make()} as usual. \code{"continue"} catches the error and stores
a failure sentinel (an object of class \code{nlmixr2targetsError}, which also
inherits from \code{"try-error"}) carrying the error message, so a single
failed model does not stop the rest of the pipeline. Detect a failed fit
with \code{inherits(fit, "nlmixr2targetsError")} or the broader
\code{inherits(fit, "try-error")}.}
}
\value{
An nlmixr2 fit object, as returned by \code{\link[nlmixr2est:nlmixr]{nlmixr2est::nlmixr()}}. When
\code{error = "continue"} and the estimation step fails, a failure sentinel of
class \code{nlmixr2targetsError} (which also inherits from \code{"try-error"}) is
returned instead; see \code{\link[=tar_nlmixr]{tar_nlmixr()}}.
}
\description{
This is the command used by the \code{fit_simple} target generated by
\code{\link[=tar_nlmixr]{tar_nlmixr()}}; it is not typically invoked by end users.
}
\details{
\code{object} is the md5 hash returned by \code{\link[=nlmixr_object_simplify]{nlmixr_object_simplify()}} and stored
as the \code{object_simple} target in the pipeline. \code{nlmixr2_indirect()} loads
the simplified \code{nlmixrui} object from the \code{nlmixr2targets} indirect cache
(under \verb{<targets store>/user/nlmixr2/}) and passes it to
\code{\link[nlmixr2est:nlmixr]{nlmixr2est::nlmixr()}} along with \code{data}, \code{est}, and \code{control}.

Routing the simplified ui through a small character-hash target rather than
a target whose value is the ui object keeps the dependency hash for the
\code{fit_simple} target independent of large rxUi internals, so cosmetic edits
to labels or metadata (which \code{\link[=nlmixr_object_simplify]{nlmixr_object_simplify()}} strips before
caching) do not invalidate the cached fit.
}
\seealso{
\code{\link[=tar_nlmixr]{tar_nlmixr()}}, \code{\link[=nlmixr_object_simplify]{nlmixr_object_simplify()}}.
}
