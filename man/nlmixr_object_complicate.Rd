% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplify.R
\name{nlmixr_object_complicate}
\alias{nlmixr_object_complicate}
\title{Re-attach labels, metadata, and original data to a simplified fit}
\usage{
nlmixr_object_complicate(fit, object, data)
}
\arguments{
\item{fit}{An estimated nlmixr2 fit produced from the simplified model.}

\item{object}{The original model function (or object) the fit was
derived from. Its labels and metadata are read back onto \code{fit}.}

\item{data}{The original data the fit corresponds to (before
\code{\link[=nlmixr_data_simplify]{nlmixr_data_simplify()}} reduced its column set).
Must have the same number of rows as the data currently stored on the
fit, mirroring \code{\link[=assign_origData]{assign_origData()}}.}
}
\value{
The modified \code{fit}.
}
\description{
The inverse of \code{\link[=nlmixr_object_simplify]{nlmixr_object_simplify()}}. Given a fit produced from the
simplified, label-and-meta-stripped model, plus the original model
function and the original data, this function:
}
\details{
\itemize{
\item re-derives parameter labels and the metadata environment from
\code{object} and writes them back onto \code{fit$ui$iniDf$label} and
\code{fit$ui$meta}, and
\item replaces \code{fit$env$origData} with the original \code{data}.
}

This is what makes label/meta edits in the source model cheap under
\code{targets}: the cache hash for the simplified model object is independent
of labels and metadata, so \code{tar_make()} only re-runs this re-attachment
step (and the cheap \verb{_object_simple} step) when only labels or metadata
change.

This function is typically not invoked directly by end users; it is the
command for the final target produced by \code{\link[=tar_nlmixr]{tar_nlmixr()}}.
}
\seealso{
\code{\link[=nlmixr_object_simplify]{nlmixr_object_simplify()}}, \code{\link[=assign_origData]{assign_origData()}}.

Other Simplifiers:
\code{\link[=nlmixr_data_simplify]{nlmixr_data_simplify()}},
\code{\link[=nlmixr_object_simplify]{nlmixr_object_simplify()}}
}
\concept{Simplifiers}
