% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplify.R
\name{nlmixr_object_zero_initial_eval}
\alias{nlmixr_object_zero_initial_eval}
\title{Runtime helper: undo the \code{cmt(0) -> cmt(initial)} rewrite before
evaluating the captured \code{object} expression.}
\usage{
nlmixr_object_zero_initial_eval(expr, envir = parent.frame())
}
\arguments{
\item{expr}{A quoted (unevaluated) language object.}

\item{envir}{The parent environment for evaluation. Defaults to
the caller's frame (the targets execution env).}
}
\value{
The result of evaluating the corrected expression.
}
\description{
Wrapped around the captured expression at construction time when
\code{tar_nlmixr_protect_zero_initial()} performed any rewrites. Needed
for pipe forms like \code{pheno |> model({...})} and \code{pheno |> ini(...)},
because nlmixr2's pipe handlers parse pheno's body before the
existing \code{nlmixr_object_simplify_zero_initial_helper()} could
intervene. Harmless for symbol-only forms (the existing helper
reaches them at simplify time anyway).
}
\details{
Mechanism:
\itemize{
\item Walk the quoted expression, rewriting
\code{name(initial) <- val} back to \code{name(0) <- val} via the existing
inverse helper.
\item Walk the (rewritten) expression for symbols. For each that
resolves to a function whose body still contains
\code{name(initial) <- val} inside \code{model({...})}, build a corrected
closure with \code{name(0) <- val} and bind it in an override frame.
\item Evaluate the rewritten expression in the override frame
(whose parent is \code{envir}), so any lookup of a rewritten symbol
hits the corrected closure first.
}

The corrected copies retain the original closure environment of
the user's function; only the body is swapped.
}
\keyword{Internal}
